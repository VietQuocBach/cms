export interface User {
    id: string
    email: string
    email_verified_at: string | null
    first_name: string
    last_name: string
    created_at: string
    updated_at: string
}

export interface UserPatch {

}